name: forge

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: forge-deploy
    uses: chainlinn/forge/.github/workflows/deploy.yml@v0.1.18
    secrets: inherit
    with:
      repo: ${{ github.event.repository.name }}
